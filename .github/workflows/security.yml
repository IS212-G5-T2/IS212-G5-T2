name: Security

on:
  push:
    branches:
      - integration
      - staging
      - main
    paths:
      - "apps/**"
      - "services/**"
      - "platform/terraform/**"
      - ".github/workflows/security.yml"
  pull_request:
    branches:
      - integration
      - staging
      - main
    paths:
      - "apps/**"
      - "services/**"
      - "platform/terraform/**"
      - ".github/workflows/security.yml"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  security-events: write

jobs:
  frontend-codeql:
    name: Frontend CodeQL
    runs-on: ubuntu-latest
    if: github.ref_name == 'integration' || github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript-typescript
          source-root: apps/frontend

      - name: Analyze
        uses: github/codeql-action/analyze@v3

  service-template-codeql:
    name: Service Template CodeQL
    runs-on: ubuntu-latest
    if: github.ref_name == 'integration' || github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript-typescript
          source-root: services/template

      - name: Analyze
        uses: github/codeql-action/analyze@v3

  terraform-iac:
    name: Terraform IaC Scan
    runs-on: ubuntu-latest
    if: github.ref_name == 'staging' || github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run Trivy IaC scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: platform/terraform
          scanners: misconfig,secret
          format: sarif
          output: trivy-results.sarif

      - name: Upload Trivy results
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-results.sarif

  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest
    if: github.ref_name == 'integration' || github.ref_name == 'staging' || github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        shell: bash
        run: |
          set -eu

          docker run --rm \
            -v "$PWD:/repo" \
            ghcr.io/gitleaks/gitleaks:v8.30.1 \
            detect --source /repo --verbose --redact
